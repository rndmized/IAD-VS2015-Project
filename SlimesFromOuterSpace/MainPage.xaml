<Page
    x:Class="SlimesFromOuterSpace.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SlimesFromOuterSpace"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Black">
        <StackPanel Orientation="Vertical">
            <Rectangle Fill="DarkKhaki" HorizontalAlignment="Stretch" Height="505" Margin="10,10,10,0" Stroke="Black" VerticalAlignment="Stretch"/>
            <StackPanel>
                <Button x:Name="btnStartGame" Background = "White" Content="Start New Game"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="11,11,11,0">
                </Button>
                <Button x:Name="btnCheckRecords" Background = "White" Content="See Records"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="11,11,11,0">
                </Button>
                <Button x:Name="btnExit" Background = "White" Content="Exit"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="11,11,11,0">
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
